# Bevy Dogfight AI

```bash
# works with ui
cargo run -- --random-ai --act-interval=12
# works in headless
cargo run -- --random-ai --headless --act-interval=12
# stats don't match (physics inaccuracy?)
cargo run -- --random-ai --headless --frameskip=12
```